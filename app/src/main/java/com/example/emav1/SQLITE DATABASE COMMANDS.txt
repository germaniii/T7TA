CREATE TABLE "CONTACTS" (
	"ID"	INTEGER,
	"NAME"	VARCHAR(128) NOT NULL,
	"NUMBER"	VARCHAR(128) NOT NULL UNIQUE,
	"CIPHER"	VARCHAR(128) NOT NULL,
	PRIMARY KEY("ID" AUTOINCREMENT)
)

CREATE TABLE "MESSAGES" (
	"MESSAGE_ID"	INTEGER,
	"SENDER_ID"	VARCHAR(128) NOT NULL,
	"MESSAGE"	TEXT NOT NULL,
	"RECEIVED"	VARCHAR(128) NOT NULL,
	"SENT"	VARCHAR(128) NOT NULL,
	PRIMARY KEY("MESSAGE_ID" AUTOINCREMENT),
	FOREIGN KEY("SENDER_ID") REFERENCES "CONTACTS"("NUMBER")
)

//---- INSERT VALUES TO MESSAGES ------//
INSERT INTO MESSAGES(SENDER_ID, MESSAGE, RECEIVED, SENT)
VALUES ("09159301068", "Hello German III 1st Message", "01/02/03", "-");

//---- RETRIEVE MESSAGE and MESSAGE DETAILS FROM SPECIFIC CONTACTS -----//
SELECT * FROM MESSAGES WHERE SENDER_ID = "09159301068"

//--- RETRIEVE ALL MESSAGES ---//
SELECT * FROM MESSAGES

//---- RETRIEVE MESSAGES AND CONTACT DETAILS FOR ALL ---//
SELECT * FROM MESSAGES 
INNER JOIN CONTACTS
ON SENDER_ID


//---- RETRIEVE MESSAGES AND CONTACT DETAILS FOR SPECIFIC CONTACTS ---//
SELECT CONTACTS.NUMBER, CONTACTS.NAME, MESSAGES.MESSAGE, MESSAGES.RECEIVED, MESSAGES.SENT
FROM MESSAGES
INNER JOIN CONTACTS
ON MESSAGES.SENDER_ID = CONTACTS.NUMBER


//---- REFERENCE VIDEO ----//
https://www.youtube.com/watch?v=uWoM3wD6eow
